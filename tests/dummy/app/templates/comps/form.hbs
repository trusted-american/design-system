{{page-title "Form"}}
{{breadcrumb "Form"}}

<Heading @title="Form" />

<Subheading @title="Form::Check" class="mt-3" />

<Form::Check
  @value={{this.check}}
  @label="Label"
  @identifier="check"
  @isRequired={{true}}
  @help="Help"
  @invalidFeedback="Invalid feedback"
/>

<Subheading @title="Form::DateInput" class="mt-3" />

<Form::DateInput
  @value={{this.date}}
  @label="Label"
  @identifier="dateInput"
  @isRequired={{true}}
  @help="Help"
  @invalidFeedback="Invalid feedback"
  @onChange={{fn (mut this.date)}}
/>

<Subheading @title="Form::FileDropzone" class="mt-3" />

<Form::FileDropzone @onCreate={{this.create}} />

<Subheading @title="Form::FileInput" class="mt-3" />

<Form::FileInput
  @label="Label"
  @identifier="file"
  @isRequired={{true}}
  @help="Help"
  @invalidFeedback="Invalid feedback"
  @onChange={{fn (mut this.file)}}
/>

<Subheading @title="Form::HtmlInput" class="mt-3" />

<Form::HtmlInput
  @value={{this.htmlValue}}
  @label="Label"
  @identifier="htmlInput"
  @isRequired={{true}}
  @help="Help"
  @invalidFeedback="Invalid feedback"
  @editorText="Editor"
  @codeText="Code"
  @onChange={{fn (mut this.htmlValue)}}
/>

<Subheading @title="Form::Input" class="mt-3" />

<Form::Input
  @value={{this.value}}
  @label="Label"
  @identifier="input"
  @isRequired={{true}}
  @help="Help"
  @invalidFeedback="Invalid feedback"
  placeholder="Placeholder"
/>

<Subheading @title="Form::NumberInput" class="mt-3" />

<Form::NumberInput
  @value={{this.number}}
  @label="Label"
  @identifier="numberInput"
  @isRequired={{true}}
  @help="Help"
  @invalidFeedback="Invalid feedback"
  @onChange={{fn (mut this.number)}}
  placeholder="Placeholder"
/>

<Subheading @title="Form::PhoneInput" class="mt-3" />

<Form::PhoneInput
  @value={{this.phone}}
  @label="Label"
  @identifier="phoneInput"
  @isRequired={{true}}
  @help="Help"
  @invalidFeedback="Invalid feedback"
  @onChange={{fn (mut this.phone)}}
  placeholder="Placeholder"
/>

<Subheading @title="Form::Radio" class="mt-3" />

<Form::Radio
  @options={{array
    (hash
      value=true
      label="Yes"
      help="With supporting text below as a natural lead-in to additional content."
    )
    (hash
      value=false
      label="No"
      help="With supporting text below as a natural lead-in to additional content."
    )
  }}
  @selected={{this.radio}}
  @label="Label"
  @identifier="radio"
  @isRequired={{true}}
  @help="Help"
  @onChange={{fn (mut this.radio)}}
/>

<Subheading @title="Form::RadioButton" class="mt-3" />

<Form::RadioButton
  @options={{array (hash value=true label="Yes") (hash value=false label="No")}}
  @selected={{this.radio}}
  @identifier="radioButton"
  @onChange={{fn (mut this.radio)}}
/>

<Subheading @title="Form::RadioCard" class="mt-3" />

<Form::RadioCard
  @options={{array
    (hash
      value=true
      label="Yes"
      icon="check"
      help="With supporting text below as a natural lead-in to additional content."
    )
    (hash
      value=false
      label="No"
      icon="xmark"
      help="With supporting text below as a natural lead-in to additional content."
    )
  }}
  @selected={{this.radio}}
  @label="Label"
  @identifier="radioCard"
  @isRequired={{true}}
  @help="Help"
  @onChange={{fn (mut this.radio)}}
/>

<Subheading @title="Form::Select" class="mt-3" />

<Form::Select
  @options={{array
    (hash value=undefined label="Choose…")
    (hash value="active" label="Active")
    (hash value="inactive" label="Inactive")
  }}
  @selected={{this.status}}
  @label="Label"
  @identifier="select"
  @isRequired={{true}}
  @help="Help"
  @onChange={{fn (mut this.status)}}
/>

<Form::Select
  @options={{array "active" "inactive"}}
  @selected={{this.status}}
  @label="Label"
  @identifier="selectSimple"
  @isRequired={{true}}
  @isSimple={{true}}
  @onChange={{fn (mut this.status)}}
/>

<Subheading @title="Form::Textarea" class="mt-3" />

<Form::Textarea
  @value={{this.value}}
  @label="Label"
  @identifier="textarea"
  @isRequired={{true}}
  @help="Help"
  placeholder="Placeholder"
/>

<Subheading @title="Form::TimeInput" class="mt-3" />

<Form::TimeInput
  @value={{this.date}}
  @label="Label"
  @identifier="timeInput"
  @isRequired={{true}}
  @help="Help"
  @onChange={{fn (mut this.date)}}
/>