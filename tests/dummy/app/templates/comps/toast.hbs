{{page-title "Toast"}}
{{breadcrumb "Toast"}}

<Heading @title="Toast" />

<Toast @closeText="Close">
  <:body>Body</:body>
</Toast>

<Toast @closeText="Close">
  <:title>Title</:title>
  <:body>Body</:body>
</Toast>

<Toast @closeText="Close" @onClose={{this.do}}>
  <:title>Title</:title>
  <:body>Body</:body>
</Toast>

<Toast @closeText="Close" @color="primary" @onClose={{this.do}}>
  <:title>Title</:title>
  <:body>Body</:body>
</Toast>

<Toast @closeText="Close" @color="success" @onClose={{this.do}}>
  <:title>Title</:title>
  <:body>Body</:body>
</Toast>

<Toast @closeText="Close" @color="warning" @onClose={{this.do}}>
  <:title>Title</:title>
  <:body>Body</:body>
</Toast>

<Toast @closeText="Close" @color="danger" @onClose={{this.do}}>
  <:title>Title</:title>
  <:body>Body</:body>
</Toast>

<Toast @closeText="Close" @onClose={{this.do}}>
  <:title>Title</:title>
  <:body>Body</:body>
  <:buttons>
    <Button @text="Button" @size="sm" @color="light" {{on "click" this.do}} />
    <Button @text="Link text" @size="sm" @color="link" {{on "click" this.do}} />
  </:buttons>
</Toast>