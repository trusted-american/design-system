{{page-title "Calendar"}}
{{breadcrumb "Calendar"}}

<Heading @title="Calendar" />

<Snippet @name="calendar">
  {{! BEGIN-SNIPPET calendar }}
  <Calendar @events={{this.model}} @onSelect={{fn (mut this.event)}} />
  {{! END-SNIPPET }}
</Snippet>

{{#if this.event}}
  <Modal @title="Event" @onClose={{this.close}}>
    {{this.event.title}}
  </Modal>
{{/if}}