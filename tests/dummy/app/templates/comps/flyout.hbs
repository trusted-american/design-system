{{page-title "Flyout"}}
{{breadcrumb "Flyout"}}

<Subheading @title="Flyout" />

<Button @text="Open flyout" {{on "click" (fn (mut this.showFlyout) true)}} />

{{#if this.showFlyout}}
  <Flyout @title="Title" @onClose={{this.close}}>
    Body
  </Flyout>
{{/if}}