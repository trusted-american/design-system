<CopyButton
  @text={{@text}}
  @container={{@container}}
  @delegateClickEvent={{@delegateClickEvent}}
  class="{{if @isButton 'btn btn-secondary'}} {{if @isFullWidth 'w-100'}}"
  data-test-copy
  ...attributes
>
  {{yield}}
</CopyButton>