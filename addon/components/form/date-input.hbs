<Form::Input
  @type={{if @isMonth "month" "date"}}
  @value={{this.value}}
  @label={{@label}}
  @identifier={{@identifier}}
  @isRequired={{true}}
  @help={{@help}}
  @invalidFeedback={{@invalidFeedback}}
  @onChange={{this.change}}
  min={{this.min}}
  max={{this.max}}
  ...attributes
>
  {{#if @isMonth}}
    1st of
  {{else}}
    <Icon @icon="calendar" />
  {{/if}}
</Form::Input>