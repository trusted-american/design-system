<Form::Label
  @text={{@label}}
  @identifier={{@identifier}}
  @required={{@required}}
/>

<Textarea
  @value={{@value}}
  id={{@identifier}}
  required={{@required}}
  class="form-control {{if @size (concat 'form-control-' @size)}}"
  ...attributes
/>

<Form::Help @text={{@help}} />