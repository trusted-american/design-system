<FaIcon
  {{! @glint-expect-error }}
  @icon={{@icon}}
  @fixedWidth={{@isFixedWidth}}
  @size={{@size}}
  @prefix={{@prefix}}
  @transform={{@transform}}
  class={{if @color (concat "text-" @color)}}
  data-test-icon
  ...attributes
/>