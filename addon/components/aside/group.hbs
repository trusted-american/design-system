<Aside::Item
  @text={{@text}}
  @icon={{@icon}}
  @route={{@route}}
  {{collapse this.id}}
/>
<Collapse class="ps-3" id={{this.id}}>
  <nav class="nav nav-pills flex-column">
    {{yield}}
  </nav>
</Collapse>