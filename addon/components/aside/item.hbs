<LinkTo @route={{@route}} class="nav-link" ...attributes>
  {{#if @icon}}
    <Icon @icon={{@icon}} @fixedWidth={{true}} class="me-2" />
  {{/if}}
  <span class={{unless @icon "ms-4"}}>{{@text}}</span>
</LinkTo>